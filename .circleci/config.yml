version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14

    steps:
      - checkout

      # Etapas de build e teste do seu projeto aqui

  copy-and-commit:
    docker:
      - image: circleci/node:14

    steps:
      - checkout

      - run:
          name: Copy files and commit
          command: |
            git config user.email "luizleno15@gmail.com"
            git config user.name "luislenosilva15"

            git checkout gh-pages

            cp -R /path/to/source/directory/* .
            git add .
            git commit -m "Copiando arquivos para nova-branch"

workflows:
  version: 2
  build-and-commit:
    jobs:
      - build
      - copy-and-commit:
          requires:
            - build
